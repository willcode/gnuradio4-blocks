// The consumer project beside this file is configured and never built: what it proves is that the exported interface
// of gnuradio4::gr-network resolves against an installed prefix. A target needs a source to have a link closure at
// all, and this is it.
int main() { return 0; }
